DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require"
JWT_SECRET="replace_me_with_strong_secret"
CORS_ORIGIN="http://localhost:5173"
# Key required when registering an ADMIN via /register (keep secret, do not commit real value)
ADMIN_SIGNUP_KEY="change_this_admin_signup_key"

# (Optional) If you want the seed script to auto-create an admin, you can define these ONLY in your local .env (do not add them here):
# ADMIN_EMAIL=you@example.com
# ADMIN_PASSWORD=StrongPassw0rd!
